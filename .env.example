GRABR_ADMIN_USER=admin
GRABR_ADMIN_PASS=change-me
GRABR_PORT=8080

# SSH target for scripts/k3s-setup.sh. Format: user@host-or-ip.
# Required (no default) — the IP is intentionally not committed.
NAS_HOST=root@your-nas-ip
